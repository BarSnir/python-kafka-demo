CREATE STREAM ADS_CAR_PRIVATE (
    after STRUCT<id INTEGER, token VARCHAR,price INTEGER>,
    before STRUCT<id INTEGER, token VARCHAR,price INTEGER>)
WITH (KAFKA_TOPIC='carprivate', KEY_FORMAT='KAFKA', VALUE_FORMAT='JSON', PARTITIONS=3);